# Files to copy from the main worktree when creating a new worktree.
# One glob per line, relative to the repository root.

.container/Dockerfile
.container/.env.app

# Add project-specific ignored files here when every worktree needs them.
# For personal-only patterns, use .worktreeinclude.local.
#
# Examples:
# .env.local
